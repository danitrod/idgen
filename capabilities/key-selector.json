{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "key-selector",
  "description": "Capability for the key selector window",
  "windows": ["key-selector"],
  "permissions": ["core:default", "core:window:allow-close"]
}
